import {StyleSheet} from "react-native";

export default styles = StyleSheet.create({
    questionText: {
        fontSize: 16,
        color: '#86939e',
        fontWeight: 'bold',
        paddingHorizontal: 10,
        paddingVertical: 10
    },
});