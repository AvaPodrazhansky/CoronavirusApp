import {StyleSheet} from "react-native";

export default styles = StyleSheet.create({
    firstQuestionText: {
        fontSize: 16,
        color: 'black',
        fontWeight: 'bold',
        paddingHorizontal: 10
    },
    questionText: {
        fontSize: 16,
        color: 'black',
        fontWeight: 'bold',
        paddingHorizontal: 10,
        paddingVertical: 10
    },
});