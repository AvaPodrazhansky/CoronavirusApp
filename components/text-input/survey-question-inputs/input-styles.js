import {StyleSheet} from "react-native";

export default styles = StyleSheet.create({
    firstQuestionInputText: {
        color: 'black'
    },
    questionInputText: {
        paddingVertical: 10,
        color: 'black'
    },
});